// calendar.js     Shared data for current trial
//
// This absurdly simple service returns a single object to be shared
// among the current trial's controllers.
//

// 'use strict';

(angular.module('tummytrials.calendar', [])
    .factory('Calendar', function() {
        return { };
    })
);
