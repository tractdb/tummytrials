// vis_data.js     Shared data for current trial
//
// This absurdly simple service returns a single object to be shared
// among the pasttrial's controllers.
//

// 'use strict';

(angular.module('tummytrials.visdata', [])
    .factory('Visdata', function() {
        return { };
    })
);
