// vis.js     Shared data for results vis and pasttrial1ctrl
//
// This absurdly simple service returns a single object to be shared
// among the past trial's controllers.
//

// 'use strict';

(angular.module('tummytrials.vis', [])
    .factory('Vis', function() {
        return { };
    })
);
