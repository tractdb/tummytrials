// setupdata.js     Shared data for study setup
//
// This absurdly simple service returns a single object to be shared
// among the study setup controllers.
//

// 'use strict';

(angular.module('tummytrials.setupdata', [])
    .factory('SetupData', function() {
        return { };
    })
);
